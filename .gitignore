.oracles/
*.bak

# journaux du hook qualité (TF-0065) : sidecars d'oracles hors dépôt — les preuves
# VOULUES vivent versionnées sous forge/ (décision C4)
*.oracles.json
*.oracles-cache.json
*.oracles-historique.jsonl
!forge/**/*.oracles.json
!forge/**/*.oracles-historique.jsonl

# TF-0579 : journaux de verdicts ops, ecrits a cote de la cible jugee. Ce sont des traces
# locales de mesure, pas des preuves versionnees — les versionner salirait le depot a chaque
# execution de l oracle sur un de ses propres fichiers (constate le 25/08, sur ce commit meme).
.ops-journal.jsonl
